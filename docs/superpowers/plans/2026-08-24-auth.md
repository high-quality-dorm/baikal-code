# Auth Subsystem (JWT/password) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add JWT/password authentication to the FastAPI app: login, first-admin bootstrap, admin user management, and role-enforcement dependencies.

**Architecture:** Extends the existing `users` table in the university DB with login/password fields. App-side auth subsystem (`app/auth`, `app/core`, `app/services`) issues JWTs carrying role + internal_id. `db_mcp` is touched only to add the `User` domain model — the db_mcp gateway/access layer is deliberately out of scope (separate roadmap stage). The auth service depends on a single `UserCredentialsStore` protocol, implemented by an in-memory mock (`InMemoryAuthStore`) for now; a real db_mcp-backed store is a follow-up.

**Tech Stack:** FastAPI, pydantic, pydantic-settings, PyJWT, bcrypt, pytest, asyncpg (schema), SQL (db/).

**Spec:** `docs/superpowers/specs/2026-08-24-auth-design.md`

## Global Constraints

- Python 3.12+; type hints: `str | None`, `list[T]`.
- Format/lint via `make format` / `make check` (ruff, ty). Run both before committing.
- Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`, `test:` (+ optional scope).
- Tests live in root `tests/`; run via `make test` (`uv run pytest tests/`).
- Dependencies added to `packages/app/pyproject.toml`; install with `uv sync --all-packages`.
- No comments unless non-obvious; Russian docstrings follow existing project style.
- db_mcp: DO NOT add access layer/server. Only add the `User` model.
- Role values (from `app/api/schemas.py`): `applicant`, `student`, `teacher`, `admin`.
- Error format: standard FastAPI `{"detail": ...}`.

---

### Task 1: DB schema — extend `users` table

**Files:**
- Modify: `db/01_schema.sql` (the `users` table definition, lines ~134-141)

**Interfaces:**
- Consumes: nothing.
- Produces: `users` table with columns `id, external_id, email, password_hash, role, internal_id, display_name, is_active`. All later tasks assume these columns exist.

- [ ] **Step 1: Update the `users` table DDL**

Replace the existing `users` block in `db/01_schema.sql`:

```sql
-- 16. Учётные записи пользователей (логин/пароль для auth + маппинг на внутренние id)
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    external_id VARCHAR(100) NOT NULL UNIQUE,
    email VARCHAR(255) UNIQUE,              -- логин (может быть NULL на время перехода)
    password_hash VARCHAR(255),             -- bcrypt-хэш пароля
    role VARCHAR(20) NOT NULL,              -- applicant | student | teacher | admin
    internal_id INT,                        -- student_id или staff_id
    display_name VARCHAR(150),
    is_active BOOLEAN NOT NULL DEFAULT TRUE
);
```

- [ ] **Step 2: Verify SQL is valid**

Run:
```
docker compose up -d --wait   # if DB not running (make db-up)
```
Restart the DB with the updated init script, then confirm the `users` table has the new columns:
```
docker compose down -v && docker compose up -d --wait
uv run python -c "import asyncio, asyncpg, os; from dotenv import load_dotenv; load_dotenv(); import re; url=os.environ['DATABASE_URL_OWNER']; " \
  # or run a manual psql check for the column set
```
Expected: `users` shows `email`, `password_hash`, `is_active` columns.

- [ ] **Step 3: Commit**

```bash
git add db/01_schema.sql
git commit -m "feat(db): add login/password columns to users table"
```

---

### Task 2: db_mcp — add `User` domain model

**Files:**
- Modify: `packages/db_mcp/src/db_mcp/models.py`

**Interfaces:**
- Consumes: nothing (standalone model).
- Produces: `User` class (exported in `__all__`), used by no other task in this plan (documentation + future gateway). Fields: `id`, `external_id`, `email`, `password_hash`, `role`, `internal_id`, `display_name`, `is_active`.

- [ ] **Step 1: Add the `User` model**

Append before `__all__` in `db_mcp/models.py`:

```python
class User(BaseModel):
    """Учётная запись пользователя (auth-данные + маппинг на внутренние id)."""

    id: int | None = None
    external_id: str = Field(min_length=1, max_length=100)
    email: str | None = Field(default=None, max_length=255)
    password_hash: str | None = Field(default=None, max_length=255)
    role: str = Field(min_length=1, max_length=20)
    internal_id: int | None = None
    display_name: str | None = Field(default=None, max_length=150)
    is_active: bool = True
```

Add `"User",` to the `__all__` list.

- [ ] **Step 2: Verify it imports and passes checks**

Run: `make check`
Expected: all checks pass (ruff, ty).

- [ ] **Step 3: Commit**

```bash
git add packages/db_mcp/src/db_mcp/models.py
git commit -m "feat(db_mcp): add User domain model"
```

---

### Task 3: app — core config (pydantic-settings)

**Files:**
- Create: `packages/app/src/app/core/config.py`
- Modify: `packages/app/src/app/core/__init__.py` (re-export if desired — optional)
- Modify: `packages/app/pyproject.toml` (add `PyJWT`, `bcrypt`, `python-dotenv` deps)

**Interfaces:**
- Consumes: nothing.
- Produces: `Settings` class with `.jwt_secret`, `.jwt_algorithm`, `.jwt_expires_minutes`. Later tasks import `get_settings()`.

- [ ] **Step 1: Add dependencies**

In `packages/app/pyproject.toml` `[project.dependencies]`, add:
```
    "PyJWT>=2.9.0",
    "bcrypt>=4.2.0",
    "python-dotenv>=1.0.1",
```

Run `uv sync --all-packages`.

- [ ] **Step 2: Write the failing test**

Create `tests/core/test_config.py`:

```python
from app.core.config import get_settings


def test_settings_defaults():
    settings = get_settings()
    assert settings.jwt_algorithm == "HS256"
    assert settings.jwt_expires_minutes > 0
```

- [ ] **Step 3: Run test to verify it fails**

Run: `make test` or `uv run pytest tests/core/test_config.py -v`
Expected: FAIL — `ModuleNotFoundError: app.core.config`.

- [ ] **Step 4: Write minimal implementation**

`packages/app/src/app/core/config.py`:

```python
"""Конфигурация приложения через pydantic-settings."""

from __future__ import annotations

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """Настройки приложения (читаются из .env / переменных окружения)."""

    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    jwt_secret: str = "dev-secret-change-me"
    jwt_algorithm: str = "HS256"
    jwt_expires_minutes: int = 60


_settings: Settings | None = None


def get_settings() -> Settings:
    """Возвращает единственный экземпляр настроек (кэш)."""
    global _settings
    if _settings is None:
        _settings = Settings()
    return _settings
```

- [ ] **Step 5: Run test to verify it passes**

Run: `uv run pytest tests/core/test_config.py -v`
Expected: PASS.

- [ ] **Step 6: Run checks and commit**

```bash
make format && make check
git add packages/app/pyproject.toml uv.lock packages/app/src/app/core/config.py tests/core/test_config.py
git commit -m "feat(app): add pydantic-settings config with JWT settings"
```

---

### Task 4: app — core security (bcrypt + JWT)

**Files:**
- Create: `packages/app/src/app/core/security.py`

**Interfaces:**
- Consumes: `get_settings()` from `core/config.py`.
- Produces:
  - `hash_password(plain: str) -> str`
  - `verify_password(plain: str, hashed: str) -> bool`
  - `create_access_token(subject: str, role: str, email: str | None = None) -> str`
  - `decode_access_token(token: str) -> dict` (raises `jwt.PyJWTError` on invalid/expired)

- [ ] **Step 1: Write the failing test**

Create `tests/core/test_security.py`:

```python
import jwt

from app.core.security import (
    create_access_token,
    decode_access_token,
    hash_password,
    verify_password,
)


def test_password_hash_roundtrip():
    hashed = hash_password("s3cret!")
    assert hashed != "s3cret!"
    assert verify_password("s3cret!", hashed)
    assert not verify_password("wrong", hashed)


def test_create_and_decode_token():
    token = create_access_token(subject="42", role="admin", email="a@b.c")
    payload = decode_access_token(token)
    assert payload["sub"] == "42"
    assert payload["role"] == "admin"
    assert payload["email"] == "a@b.c"


def test_decode_raises_on_bad_token():
    try:
        decode_access_token("not-a-jwt")
        assert False, "expected error"
    except jwt.PyJWTError:
        pass
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/core/test_security.py -v`
Expected: FAIL — `ModuleNotFoundError: app.core.security`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/core/security.py`:

```python
"""Хеширование паролей и работа с JWT-токенами."""

from __future__ import annotations

from datetime import datetime, timedelta, timezone

import bcrypt
import jwt

from .config import get_settings


def hash_password(plain: str) -> str:
    """Возвращает bcrypt-хэш пароля."""
    return bcrypt.hashpw(plain.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verify_password(plain: str, hashed: str) -> bool:
    """Проверяет пароль против bcrypt-хэша."""
    try:
        return bcrypt.checkpw(plain.encode("utf-8"), hashed.encode("utf-8"))
    except ValueError:
        return False


def create_access_token(subject: str, role: str, email: str | None = None) -> str:
    """Создаёт JWT access-токен с ролью и внутренним id."""
    settings = get_settings()
    now = datetime.now(timezone.utc)
    payload = {
        "sub": subject,
        "role": role,
        "iat": now,
        "exp": now + timedelta(minutes=settings.jwt_expires_minutes),
    }
    if email is not None:
        payload["email"] = email
    return jwt.encode(payload, settings.jwt_secret, algorithm=settings.jwt_algorithm)


def decode_access_token(token: str) -> dict:
    """Декодирует JWT; при невалидном/просроченном токене бросает jwt.PyJWTError."""
    settings = get_settings()
    return jwt.decode(token, settings.jwt_secret, algorithms=[settings.jwt_algorithm])
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/core/test_security.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/app/src/app/core/security.py tests/core/test_security.py
git commit -m "feat(app): add bcrypt password hashing and JWT helpers"
```

---

### Task 5: app — auth schemas (pydantic)

**Files:**
- Create: `packages/app/src/app/auth/schemas.py`

**Interfaces:**
- Consumes: `Role` from `app.api.schemas`.
- Produces pydantic models:
  - `LoginRequest` (`email: str`, `password: str`)
  - `TokenResponse` (`access_token: str`, `token_type: str = "bearer"`, `role: str`)
  - `UserCreate` (`email: str`, `password: str`, `role: Role`, `external_id: str | None`, `display_name: str | None`)
  - `UserUpdate` (`role: Role | None`, `is_active: bool | None`, `password: str | None`, `display_name: str | None`)
  - `UserOut` (`id: int`, `email: str | None`, `external_id: str`, `role: str`, `internal_id: int | None`, `display_name: str | None`, `is_active: bool`)
  - `Credentials` (internal: `id: int`, `email: str | None`, `external_id: str`, `password_hash: str | None`, `role: str`, `internal_id: int | None`, `display_name: str | None`, `is_active: bool`)

- [ ] **Step 1: Write the failing test**

Create `tests/auth/test_schemas.py`:

```python
from pydantic import ValidationError
import pytest

from app.auth.schemas import LoginRequest, UserCreate, UserOut
from app.api.schemas import Role


def test_login_request_valid():
    req = LoginRequest(email="a@b.c", password="pw")
    assert req.email == "a@b.c"


def test_user_create_requires_role():
    user = UserCreate(email="a@b.c", password="pw", role=Role.ADMIN)
    assert user.role == Role.ADMIN


def test_user_out_optional_fields():
    u = UserOut(id=1, external_id="ext", role="student", is_active=True)
    assert u.email is None
    assert u.internal_id is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/auth/test_schemas.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.schemas`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/auth/schemas.py`:

```python
"""Pydantic-схемы auth: тела запросов/ответов и внутренние данные учётки."""

from __future__ import annotations

from pydantic import BaseModel, EmailStr, Field

from app.api.schemas import Role


class LoginRequest(BaseModel):
    """Тело запроса на вход."""

    email: str = Field(min_length=1, max_length=255)
    password: str = Field(min_length=1, max_length=128)


class TokenResponse(BaseModel):
    """Ответ с access-токеном."""

    access_token: str
    token_type: str = "bearer"
    role: str


class Credentials(BaseModel):
    """Данные учётки, возвращаемые хранилищем (id присваивает хранилище)."""

    id: int | None = None
    email: str | None = None
    external_id: str
    password_hash: str | None = None
    role: str
    internal_id: int | None = None
    display_name: str | None = None
    is_active: bool = True


class UserCreate(BaseModel):
    """Создание учётки админом."""

    email: str = Field(min_length=3, max_length=255)
    password: str = Field(min_length=8, max_length=128)
    role: Role
    external_id: str | None = Field(default=None, max_length=100)
    display_name: str | None = Field(default=None, max_length=150)


class UserUpdate(BaseModel):
    """Обновление учётки админом (все поля необязательны)."""

    role: Role | None = None
    is_active: bool | None = None
    password: str | None = Field(default=None, min_length=8, max_length=128)
    display_name: str | None = Field(default=None, max_length=150)


class UserOut(BaseModel):
    """Представление учётки для ответа (без password_hash)."""

    id: int
    email: str | None = None
    external_id: str
    role: str
    internal_id: int | None = None
    display_name: str | None = None
    is_active: bool = True
```

Note: `EmailStr` requires `email-validator`; if it is not available, replace `email: EmailStr` usage with plain `str`. This plan uses `str` for `email` in `LoginRequest`/`UserCreate` to avoid an extra dependency — do not add `email-validator` unless needed.

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/auth/test_schemas.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/app/src/app/auth/schemas.py tests/auth/test_schemas.py
git commit -m "feat(app): add auth pydantic schemas"
```

---

### Task 6: app — UserCredentialsStore protocol + in-memory store

**Files:**
- Create: `packages/app/src/app/services/providers.py`

**Interfaces:**
- Consumes: `Credentials` from `app.auth.schemas`.
- Produces:
  - `class UserCredentialsStore(Protocol)` — async methods: `get_credentials(login) -> Credentials | None`, `find(user_id: int) -> Credentials | None`, `all() -> list[Credentials]`, `persist(user: Credentials) -> Credentials`.
  - `class InMemoryAuthStore` — in-memory implementation of the protocol. Also exposes `add(user: Credentials) -> Credentials` (alias for `persist`, used for seeding).

This is the single abstraction `AuthService` depends on. A real db_mcp-backed implementation is a later stage.

- [ ] **Step 1: Write the failing test**

Create `tests/services/test_providers.py`:

```python
from app.auth.schemas import Credentials
from app.services.providers import InMemoryAuthStore


def test_persist_assigns_id_and_find():
    store = InMemoryAuthStore()
    user = Credentials(id=None, external_id="e1", email="a@b.c",
                       password_hash="h", role="student", is_active=True)
    saved = store.persist(user)
    assert saved.id is not None
    assert store.find(saved.id) is saved


def test_find_missing():
    store = InMemoryAuthStore()
    assert store.find(999) is None


def test_get_credentials_by_email():
    store = InMemoryAuthStore()
    store.add(Credentials(id=1, external_id="e1", email="a@b.c",
                          password_hash="h", role="student", is_active=True))
    creds = store.get_credentials("a@b.c")
    assert creds is not None
    assert creds.role == "student"


def test_get_credentials_by_external_id():
    store = InMemoryAuthStore()
    store.add(Credentials(id=2, external_id="ext-2", email=None,
                          password_hash="h", role="teacher", is_active=True))
    creds = store.get_credentials("ext-2")
    assert creds is not None
    assert creds.id == 2


def test_get_credentials_missing():
    store = InMemoryAuthStore()
    assert store.get_credentials("nobody") is None


def test_all_lists_users():
    store = InMemoryAuthStore()
    store.add(Credentials(id=None, external_id="e1", email="a@b.c",
                          password_hash="h", role="student", is_active=True))
    store.add(Credentials(id=None, external_id="e2", email="b@b.c",
                          password_hash="h", role="admin", is_active=True))
    assert len(store.all()) == 2
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/services/test_providers.py -v`
Expected: FAIL — `ModuleNotFoundError: app.services.providers`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/services/providers.py`:

```python
"""Хранилище учётных записей для auth-сервиса.

Реальное хранилище будет получать данные через db_mcp (этап «ядро db_mcp»).
Сейчас используется InMemoryAuthStore (мок) для разработки и тестов.
"""

from __future__ import annotations

from typing import Protocol

from app.auth.schemas import Credentials


class UserCredentialsStore(Protocol):
    """Интерфейс хранилища учётных записей, которым пользуется AuthService."""

    async def get_credentials(self, login: str) -> Credentials | None: ...

    async def find(self, user_id: int) -> Credentials | None: ...

    async def all(self) -> list[Credentials]: ...

    async def persist(self, user: Credentials) -> Credentials: ...


class InMemoryAuthStore:
    """Мок-хранилище учёток в памяти (по id и по логину: email или external_id)."""

    def __init__(self) -> None:
        self._users: dict[int, Credentials] = {}
        self._by_login: dict[str, int] = {}
        self._next_id = 1

    async def add(self, user: Credentials) -> Credentials:
        """Регистрирует учётку (алиас persist, удобен для сидинга в тестах)."""
        return await self.persist(user)

    async def persist(self, user: Credentials) -> Credentials:
        """Сохраняет учётку; присваивает id, если его ещё нет."""
        if user.id is None or user.id == 0:
            user = user.model_copy(update={"id": self._next_id})
            self._next_id += 1
        self._users[user.id] = user
        if user.email:
            self._by_login[user.email] = user.id
        self._by_login[user.external_id] = user.id
        return user

    async def find(self, user_id: int) -> Credentials | None:
        """Возвращает учётку по id или None."""
        return self._users.get(user_id)

    async def all(self) -> list[Credentials]:
        """Возвращает все учётки."""
        return list(self._users.values())

    async def get_credentials(self, login: str) -> Credentials | None:
        """Возвращает учётку по логину (email или external_id) или None."""
        user_id = self._by_login.get(login)
        if user_id is None:
            return None
        return self._users.get(user_id)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/services/test_providers.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/app/src/app/services/providers.py tests/services/test_providers.py
git commit -m "feat(app): add UserCredentialsStore protocol and in-memory store"
```

---

### Task 7: app — auth service

**Files:**
- Create: `packages/app/src/app/services/auth.py`

**Interfaces:**
- Consumes:
  - `UserCredentialsStore` (protocol) from `app.services.providers`
  - `Credentials`, `TokenResponse`, `UserCreate`, `UserOut`, `UserUpdate` from `app.auth.schemas`
  - `hash_password`, `verify_password`, `create_access_token` from `app.core.security`
- Produces `AuthService` class (constructor takes `store: UserCredentialsStore`):
  - `async authenticate(login: str, password: str) -> TokenResponse` (raises `AuthenticationError`)
  - `async bootstrap_admin(email: str, password: str) -> UserOut` (raises `AdminExistsError`)
  - `async create_user(data: UserCreate) -> UserOut` (raises `DuplicateLoginError`)
  - `async list_users() -> list[UserOut]`
  - `async get_user(user_id: int) -> UserOut | None`
  - `async update_user(user_id: int, data: UserUpdate) -> UserOut | None`
  - `async deactivate_user(user_id: int) -> bool`
- Exceptions: `AuthError`, `AuthenticationError`, `AdminExistsError`, `DuplicateLoginError`.

- [ ] **Step 1: Write the failing test**

Create `tests/services/test_auth.py`:

```python
import pytest

from app.auth.schemas import Credentials, UserCreate, UserUpdate
from app.api.schemas import Role
from app.core.security import hash_password
from app.services.auth import (
    AdminExistsError,
    AuthenticationError,
    AuthService,
    DuplicateLoginError,
)
from app.services.providers import InMemoryAuthStore


def make_service():
    return AuthService(InMemoryAuthStore())


def test_authenticate_success():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="admin", is_active=True))
    token = svc.authenticate("a@b.c", "pw123456")
    assert token.access_token
    assert token.role == "admin"


def test_authenticate_wrong_password():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="admin", is_active=True))
    with pytest.raises(AuthenticationError):
        svc.authenticate("a@b.c", "badpass")


def test_authenticate_inactive():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="admin", is_active=False))
    with pytest.raises(AuthenticationError):
        svc.authenticate("a@b.c", "pw123456")


def test_authenticate_unknown_login():
    svc = make_service()
    with pytest.raises(AuthenticationError):
        svc.authenticate("nobody", "whatever1")


def test_bootstrap_admin_creates_when_empty():
    svc = make_service()
    out = svc.bootstrap_admin("admin@x.ru", "admin12345")
    assert out.role == Role.ADMIN.value


def test_bootstrap_admin_conflict():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="admin", is_active=True))
    with pytest.raises(AdminExistsError):
        svc.bootstrap_admin("admin2@x.ru", "admin12345")


def test_create_user_duplicate():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="student", is_active=True))
    with pytest.raises(DuplicateLoginError):
        svc.create_user(UserCreate(email="a@b.c", password="pw123456", role=Role.STUDENT))


def test_update_user_password():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("oldpass1"), role="student", is_active=True))
    svc.update_user(1, UserUpdate(password="newpass1"))
    assert svc.authenticate("a@b.c", "newpass1").role == "student"


def test_deactivate_user():
    svc = make_service()
    svc.store.add(Credentials(
        id=None, external_id="e1", email="a@b.c",
        password_hash=hash_password("pw123456"), role="student", is_active=True))
    assert svc.deactivate_user(1) is True
    with pytest.raises(AuthenticationError):
        svc.authenticate("a@b.c", "pw123456")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/services/test_auth.py -v`
Expected: FAIL — `ModuleNotFoundError: app.services.auth`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/services/auth.py`:

```python
"""Сервис аутентификации: вход, bootstrap-админ, управление учётками.

Зависит от UserCredentialsStore (реальная реализация — через db_mcp, потом).
"""

from __future__ import annotations

from app.auth.schemas import (
    Credentials,
    TokenResponse,
    UserCreate,
    UserOut,
    UserUpdate,
)
from app.core.security import (
    create_access_token,
    hash_password,
    verify_password,
)
from app.services.providers import UserCredentialsStore


class AuthError(Exception):
    """Базовая ошибка auth."""


class AuthenticationError(AuthError):
    """Неверные учётные данные."""


class AdminExistsError(AuthError):
    """Админ уже существует (bootstrap невозможен)."""


class DuplicateLoginError(AuthError):
    """Логин уже занят."""


class AuthService:
    """Оркестрирует аутентификацию и управление учётными записями."""

    def __init__(self, store: UserCredentialsStore) -> None:
        self.store = store

    async def authenticate(self, login: str, password: str) -> TokenResponse:
        """Проверяет логин/пароль и выдаёт JWT."""
        creds = await self.store.get_credentials(login)
        if creds is None or not creds.is_active:
            raise AuthenticationError("Неверный логин или пароль")
        if not creds.password_hash or not verify_password(password, creds.password_hash):
            raise AuthenticationError("Неверный логин или пароль")
        subject = str(creds.internal_id) if creds.internal_id is not None else str(creds.id)
        token = create_access_token(
            subject=subject,
            role=creds.role,
            email=creds.email,
        )
        return TokenResponse(access_token=token, role=creds.role)

    async def bootstrap_admin(self, email: str, password: str) -> UserOut:
        """Создаёт первого админа, только если админов ещё нет."""
        all_users = await self.list_users()
        if any(u.role == "admin" for u in all_users):
            raise AdminExistsError("Админ уже существует")
        return await self.create_user(
            UserCreate(email=email, password=password, role="admin")
        )

    async def create_user(self, data: UserCreate) -> UserOut:
        """Создаёт учётку (email уникален)."""
        existing = await self.store.get_credentials(data.email)
        if existing is not None:
            raise DuplicateLoginError("Логин уже занят")
        user = await self.store.persist(
            Credentials(
                id=None,
                external_id=data.external_id or f"user-{data.email}",
                email=data.email,
                password_hash=hash_password(data.password),
                role=data.role.value if hasattr(data.role, "value") else str(data.role),
                display_name=data.display_name,
                is_active=True,
            )
        )
        return self._to_out(user)

    async def list_users(self) -> list[UserOut]:
        """Возвращает все учётки."""
        return [self._to_out(c) for c in await self.store.all()]

    async def get_user(self, user_id: int) -> UserOut | None:
        """Возвращает учётку по id или None."""
        user = await self.store.find(user_id)
        return self._to_out(user) if user else None

    async def update_user(self, user_id: int, data: UserUpdate) -> UserOut | None:
        """Обновляет учётку; при смене пароля перехеширует его."""
        user = await self.store.find(user_id)
        if user is None:
            return None
        if data.role is not None:
            user.role = data.role.value if hasattr(data.role, "value") else str(data.role)
        if data.is_active is not None:
            user.is_active = data.is_active
        if data.display_name is not None:
            user.display_name = data.display_name
        if data.password is not None:
            user.password_hash = hash_password(data.password)
        await self.store.persist(user)
        return self._to_out(user)

    async def deactivate_user(self, user_id: int) -> bool:
        """Деактивирует учётку (мягкое удаление)."""
        user = await self.store.find(user_id)
        if user is None:
            return False
        user.is_active = False
        await self.store.persist(user)
        return True

    @staticmethod
    def _to_out(user: Credentials) -> UserOut:
        return UserOut(
            id=user.id,
            email=user.email,
            external_id=user.external_id,
            role=user.role,
            internal_id=user.internal_id,
            display_name=user.display_name,
            is_active=user.is_active,
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/services/test_auth.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/app/src/app/services/auth.py tests/services/test_auth.py
git commit -m "feat(app): add auth service with login and user management"
```

---

### Task 9: app — auth dependencies (get_current_user, require_role)

**Files:**
- Create: `packages/app/src/app/auth/deps.py`

**Interfaces:**
- Consumes: `decode_access_token` from `app.core.security`; `get_settings` not needed directly.
- Produces:
  - `class AuthContext` (dataclass: `user_id: int | None`, `role: str | None`, `email: str | None`)
  - `def get_current_user(authorization: str = Header(...)) -> AuthContext` (raises HTTPException 401)
  - `def require_role(*roles: str) -> Callable` returning a dependency that checks role (HTTPException 403)
  - A `get_auth_context` dependency that tolerates missing/invalid token (returns anonymous `AuthContext`) for public endpoints.

- [ ] **Step 1: Write the failing test**

Create `tests/auth/test_deps.py`:

```python
from fastapi import HTTPException
import pytest

from app.auth.deps import AuthContext, get_current_user, require_role
from app.core.security import create_access_token


def make_headers(role="admin", user_id="7"):
    token = create_access_token(subject=user_id, role=role, email="a@b.c")
    return {"Authorization": f"Bearer {token}"}


def test_get_current_user_ok():
    ctx = get_current_user(make_headers()["Authorization"])
    assert ctx.role == "admin"
    assert ctx.user_id == "7"


def test_get_current_user_missing_header():
    with pytest.raises(HTTPException) as ei:
        get_current_user(None)
    assert ei.value.status_code == 401


def test_require_role_allows():
    dep = require_role("admin")
    ctx = get_current_user(make_headers(role="admin")["Authorization"])
    assert dep(ctx) is None


def test_require_role_denies():
    dep = require_role("admin")
    ctx = get_current_user(make_headers(role="student")["Authorization"])
    with pytest.raises(HTTPException) as ei:
        dep(ctx)
    assert ei.value.status_code == 403
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/auth/test_deps.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.deps`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/auth/deps.py`:

```python
"""FastAPI-зависимости аутентификации и проверки ролей."""

from __future__ import annotations

from dataclasses import dataclass
from typing import Callable

import jwt
from fastapi import Header, HTTPException

from app.core.security import decode_access_token


@dataclass
class AuthContext:
    """Идентичность текущего пользователя из JWT."""

    user_id: str | None = None
    role: str | None = None
    email: str | None = None


def get_current_user(authorization: str | None = Header(default=None)) -> AuthContext:
    """Разбирает Bearer-токен; при отсутствии/невалидности — 401."""
    if not authorization or not authorization.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="Требуется авторизация")
    token = authorization.removeprefix("Bearer ").strip()
    try:
        payload = decode_access_token(token)
    except jwt.PyJWTError:
        raise HTTPException(status_code=401, detail="Невалидный или просроченный токен")
    return AuthContext(
        user_id=payload.get("sub"),
        role=payload.get("role"),
        email=payload.get("email"),
    )


def get_optional_context(authorization: str | None = Header(default=None)) -> AuthContext:
    """То же, но аноним при отсутствии/невалидном токене (для публичных эндпоинтов)."""
    if not authorization or not authorization.startswith("Bearer "):
        return AuthContext()
    token = authorization.removeprefix("Bearer ").strip()
    try:
        payload = decode_access_token(token)
    except jwt.PyJWTError:
        return AuthContext()
    return AuthContext(
        user_id=payload.get("sub"),
        role=payload.get("role"),
        email=payload.get("email"),
    )


def require_role(*roles: str) -> Callable[[AuthContext], None]:
    """Возвращает зависимость, требующую одну из указанных ролей (иначе 403)."""

    def checker(ctx: AuthContext) -> None:
        if not ctx.role or ctx.role not in roles:
            raise HTTPException(status_code=403, detail="Недостаточно прав")
        return None

    return checker
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/auth/test_deps.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/app/src/app/auth/deps.py tests/auth/test_deps.py
git commit -m "feat(app): add auth dependencies for current user and role checks"
```

---

### Task 10: app — auth router + FastAPI wiring

**Files:**
- Create: `packages/app/src/app/auth/router.py`
- Create: `packages/app/src/app/api/router.py` (top-level API router including auth)
- Modify: `packages/app/src/app/main.py` (create FastAPI app, include routers) — note: currently no `main.py` exists in `packages/app/src/app`; create it.
- Modify: `packages/app/src/app/__init__.py` (optional re-export of `create_app`)

**Interfaces:**
- Consumes: `AuthService`, `InMemoryAuthStore`, `get_current_user`, `require_role`, `get_optional_context`, auth schemas, `Role` enum.
- Produces:
  - `def create_app() -> FastAPI` — app factory; wires dependency override for `AuthService`.
  - `def build_auth_service() -> AuthService` — constructs service with `InMemoryAuthStore`.
  - Router endpoints as specced.

- [ ] **Step 1: Write the failing test**

Create `tests/auth/test_api.py`:

```python
from fastapi.testclient import TestClient

from app.main import create_app
from app.core.security import hash_password
from app.auth.schemas import Credentials
from app.services.providers import InMemoryAuthStore
from app.services.auth import AuthService


def make_client():
    app = create_app()
    return TestClient(app)


def test_login_success():
    client = make_client()
    client.post("/api/v1/auth/bootstrap-admin",
                json={"email": "admin@x.ru", "password": "admin12345"})
    resp = client.post("/api/v1/auth/login",
                       json={"email": "admin@x.ru", "password": "admin12345"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["access_token"]
    assert body["role"] == "admin"


def test_login_wrong_password():
    client = make_client()
    client.post("/api/v1/auth/bootstrap-admin",
                json={"email": "admin@x.ru", "password": "admin12345"})
    resp = client.post("/api/v1/auth/login",
                       json={"email": "admin@x.ru", "password": "wrongpass"})
    assert resp.status_code == 401


def test_bootstrap_admin_only_once():
    client = make_client()
    r1 = client.post("/api/v1/auth/bootstrap-admin",
                     json={"email": "a@x.ru", "password": "admin12345"})
    r2 = client.post("/api/v1/auth/bootstrap-admin",
                     json={"email": "b@x.ru", "password": "admin12345"})
    assert r1.status_code == 200
    assert r2.status_code == 409


def test_admin_can_create_user():
    client = make_client()
    client.post("/api/v1/auth/bootstrap-admin",
                json={"email": "admin@x.ru", "password": "admin12345"})
    login = client.post("/api/v1/auth/login",
                        json={"email": "admin@x.ru", "password": "admin12345"}).json()
    headers = {"Authorization": f"Bearer {login['access_token']}"}
    resp = client.post("/api/v1/auth/users",
                       json={"email": "stud@x.ru", "password": "stud12345", "role": "student"},
                       headers=headers)
    assert resp.status_code == 201


def test_non_admin_cannot_create_user():
    client = make_client()
    client.post("/api/v1/auth/bootstrap-admin",
                json={"email": "admin@x.ru", "password": "admin12345"})
    login = client.post("/api/v1/auth/login",
                        json={"email": "admin@x.ru", "password": "admin12345"}).json()
    # create a student via admin
    headers = {"Authorization": f"Bearer {login['access_token']}"}
    client.post("/api/v1/auth/users",
                json={"email": "stud@x.ru", "password": "stud12345", "role": "student"},
                headers=headers)
    slogin = client.post("/api/v1/auth/login",
                         json={"email": "stud@x.ru", "password": "stud12345"}).json()
    sheaders = {"Authorization": f"Bearer {slogin['access_token']}"}
    resp = client.post("/api/v1/auth/users",
                       json={"email": "x@x.ru", "password": "xxxx12345", "role": "student"},
                       headers=sheaders)
    assert resp.status_code == 403


def test_anonymous_gets_401_on_admin_endpoint():
    client = make_client()
    resp = client.get("/api/v1/auth/users")
    assert resp.status_code == 401
```

Note: `create_app` must use a shared, per-app `AuthService` instance so bootstrap + login share the same in-memory store across requests. Wire it via a FastAPI dependency.

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/auth/test_api.py -v`
Expected: FAIL — `ModuleNotFoundError: app.main`.

- [ ] **Step 3: Write minimal implementation**

`packages/app/src/app/auth/router.py`:

```python
"""HTTP-роутер аутентификации и управления учётными записями."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException

from app.api.schemas import Role
from app.auth.deps import AuthContext, get_current_user, require_role
from app.auth.schemas import LoginRequest, TokenResponse, UserCreate, UserOut, UserUpdate
from app.services.auth import (
    AdminExistsError,
    AuthenticationError,
    AuthService,
    DuplicateLoginError,
)

router = APIRouter(prefix="/api/v1/auth", tags=["auth"])


def _service_dep() -> AuthService:
    """Возвращает сервис auth; переопределяется в create_app для общего инстанса."""
    raise NotImplementedError


@router.post("/login", response_model=TokenResponse)
async def login(data: LoginRequest, service: AuthService = Depends(_service_dep)):
    try:
        return await service.authenticate(data.email, data.password)
    except AuthenticationError as exc:
        raise HTTPException(status_code=401, detail=str(exc))


@router.post("/bootstrap-admin", response_model=UserOut, status_code=200)
async def bootstrap_admin(data: UserCreate, service: AuthService = Depends(_service_dep)):
    try:
        return await service.bootstrap_admin(data.email, data.password)
    except AdminExistsError as exc:
        raise HTTPException(status_code=409, detail=str(exc))


@router.post("/users", response_model=UserOut, status_code=201,
             dependencies=[Depends(require_role("admin"))])
async def create_user(data: UserCreate, service: AuthService = Depends(_service_dep)):
    try:
        return await service.create_user(data)
    except DuplicateLoginError as exc:
        raise HTTPException(status_code=409, detail=str(exc))


@router.get("/users", response_model=list[UserOut],
            dependencies=[Depends(require_role("admin"))])
async def list_users(service: AuthService = Depends(_service_dep)):
    return await service.list_users()


@router.get("/users/me", response_model=UserOut)
async def my_user(ctx: AuthContext = Depends(get_current_user),
                  service: AuthService = Depends(_service_dep)):
    user_id = int(ctx.user_id) if ctx.user_id and ctx.user_id.isdigit() else None
    user = await service.get_user(user_id) if user_id else None
    if user is None:
        raise HTTPException(status_code=404, detail="Пользователь не найден")
    return user


@router.patch("/users/{user_id}", response_model=UserOut,
              dependencies=[Depends(require_role("admin"))])
async def update_user(user_id: int, data: UserUpdate,
                      service: AuthService = Depends(_service_dep)):
    user = await service.update_user(user_id, data)
    if user is None:
        raise HTTPException(status_code=404, detail="Пользователь не найден")
    return user


@router.delete("/users/{user_id}", status_code=204,
               dependencies=[Depends(require_role("admin"))])
async def deactivate_user(user_id: int, service: AuthService = Depends(_service_dep)):
    ok = await service.deactivate_user(user_id)
    if not ok:
        raise HTTPException(status_code=404, detail="Пользователь не найден")
```

`packages/app/src/app/main.py`:

```python
"""Точка входа FastAPI-приложения."""

from __future__ import annotations

from fastapi import FastAPI

from app.auth.router import _service_dep, router as auth_router
from app.services.auth import AuthService
from app.services.providers import InMemoryAuthStore


def build_auth_service() -> AuthService:
    """Собирает сервис auth на in-memory хранилище (до подключения db_mcp)."""
    return AuthService(InMemoryAuthStore())


def create_app() -> FastAPI:
    """Создаёт и настраивает приложение FastAPI."""
    app = FastAPI(title="Baikal")
    app.include_router(auth_router)

    def _override():
        return build_auth_service()

    app.dependency_overrides[_service_dep] = _override
    return app


app = create_app()
```

Note: `AuthService.__init__(store: UserCredentialsStore)` (Task 7) already accepts the store; `InMemoryAuthStore` (Task 6) implements the full protocol, so this wiring works as-is.

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/auth/test_api.py -v`
Expected: PASS.

- [ ] **Step 5: Run checks and commit**

```bash
make format && make check
git add packages/app/src/app/auth/router.py packages/app/src/app/main.py tests/auth/test_api.py
git commit -m "feat(app): add auth router and FastAPI app wiring"
```

---

### Task 11: docs update

**Files:**
- Modify: `docs/decisions.md` (update ADR #4, #9)
- Modify: `docs/architecture.md` (auth section)
- Modify: `docs/roadmap.md` (mark auth done)

- [ ] **Step 1: Update decisions.md**

Replace ADR #4 text:

```markdown
## 4. Аутентификация: JWT + пароль (bcrypt)
- **Решение:** аутентификация по логину/паролю с выдачей JWT access-токена (HS256).
  Учётные записи хранятся в расширенной таблице `users` (email/password_hash/is_active).
- **Почему:** полноценная аутентификация вместо прототипных заголовков `X-Role`/`X-User-Id`.
  Роль и internal_id берутся из JWT.
```

Update ADR #9 to note the extended columns.

- [ ] **Step 2: Update architecture.md**

Replace the header-based auth line (line ~50) with a JWT auth description and note the `users` columns.

- [ ] **Step 3: Update roadmap.md**

Move auth to the completed table and update "Что дальше".

- [ ] **Step 4: Run checks and commit**

```bash
make check
git add docs/decisions.md docs/architecture.md docs/roadmap.md
git commit -m "docs(auth): update decisions, architecture, and roadmap for JWT auth"
```

---

## Self-Review

- **Spec coverage:** Schema extension (Task 1) ✓; db_mcp User model (Task 2) ✓; config/security (3-4) ✓; auth service + protocol/mock (6-8) ✓; deps (9) ✓; router + endpoints + app wiring (10) ✓; docs (11) ✓. Out-of-scope items (RLS query rules, SSO) correctly excluded.
- **Placeholders:** All steps contain concrete code; no TBD/TODO.
- **Type consistency:** `Credentials`, `UserOut`, `UserCreate`, `UserUpdate`, `TokenResponse`, `AuthContext`, `AuthService` method names are consistent across tasks. `AuthService.__init__` accepts a `UserCredentialsStore`; `InMemoryAuthStore` (Task 6) implements the protocol, `providers.py` is created before `auth.py` (Task 7) uses it and before `main.py` (Task 10) wires it.
- **Known follow-ups (explicitly out of scope):** real db_mcp-backed `UserCredentialsProvider`; query authorization per role; JWT↔RLS wiring.
